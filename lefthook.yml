pre-commit:
  commands:
    linter:
      files: git diff --name-only @{push}
      glob: "*.{js,css}"
      run: yarn lint {files} --fix

    prettier:
      files: git diff --name-only @{push}
      glob: "*.{js,css}"
      run: prettier {files} --write

    add:
      run: git add .
